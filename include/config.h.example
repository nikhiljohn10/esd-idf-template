#ifndef CONFIG_H
#define CONFIG_H

// Copy this file to config.h and fill in your values.
// config.h is gitignored — credentials will never be committed.

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-password"

#endif // CONFIG_H
